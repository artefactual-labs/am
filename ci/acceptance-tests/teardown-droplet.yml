---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: '2.1'

inputs:
- name: config

params:
  DROPLET_NAME:

run:
  path: bash
  args:
  - -c
  - |
    gem install --silent tugboat

    cp config/.tugboat .

    # Disabled so I can debug it.
    # tugboat destroy --confirm $DROPLET_NAME
